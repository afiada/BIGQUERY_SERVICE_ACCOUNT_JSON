name: Simple BigQuery Test

on: [workflow_dispatch]

jobs:
  simple-test:
    runs-on: ubuntu-latest
    steps:
    - name: Test Secrets
      run: |
        echo "GCP Project: ${{ secrets.GCP_PROJECT_ID }}"
        echo "JSON exists: ${{ secrets.BIGQUERY_SERVICE_ACCOUNT_JSON != '' }}"
